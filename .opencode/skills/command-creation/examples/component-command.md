---
description: Create React component
---

Create a new React component named $1:

Component template to follow:
@templates/Component.tsx

Requirements:
1. Create component file: src/components/$1/$1.tsx
2. Create test file: src/components/$1/$1.test.tsx
3. Include TypeScript types and props interface
4. Add basic styling with CSS modules
5. Export from index: src/components/$1/index.ts

Follow these patterns:
- Use functional components with hooks
- Implement proper prop types
- Include JSDoc comments
- Add accessibility attributes
- Create basic unit tests
